/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FilmRentalHistoryRequest } from '../../models/film-rental-history-request';
import { FilmRentalHistoryResponse } from '../../models/film-rental-history-response';

export interface CreateRentalHistory$Params {
      body: FilmRentalHistoryRequest
}

export function createRentalHistory(http: HttpClient, rootUrl: string, params: CreateRentalHistory$Params, context?: HttpContext): Observable<StrictHttpResponse<FilmRentalHistoryResponse>> {
  const rb = new RequestBuilder(rootUrl, createRentalHistory.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FilmRentalHistoryResponse>;
    })
  );
}

createRentalHistory.PATH = '/film-rental-histories';
