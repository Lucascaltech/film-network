/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseRentedFilmResponse } from '../../models/page-response-rented-film-response';

export interface GetBorrowedFilms$Params {
  page?: number;
  size?: number;
}

export function getBorrowedFilms(http: HttpClient, rootUrl: string, params?: GetBorrowedFilms$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseRentedFilmResponse>> {
  const rb = new RequestBuilder(rootUrl, getBorrowedFilms.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseRentedFilmResponse>;
    })
  );
}

getBorrowedFilms.PATH = '/films/user/borrowed';
